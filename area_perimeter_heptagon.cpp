// Copyright (c) 2025 Adwok Adiebo All rights reserved.
//.
// Created by: Adwok Adiebo
// Created on: Mar 7th, 2025
// This program calculates the area and
// perimeter of a heptagon.
# include <cmath>
# include <iostream>

int main(){
    // The initial greeting
    std::cout << "Hello & Welcome to Adwok's Heptagon formula !" << std::endl;
    // Input
    int a = ((7 / 4) * pow(a, 2) * (1 / tan * ((180/7))));
    // Process
    float area = (7 / 4) * pow(a,2) *(1 / tan * (degree *(180/7)));
    int perimeter = (7 * a)
    // Output
    std::cout:: <<"The area of the heptagon is: {}cm^2" << format(area) <<
    // The Perimeter if the Heptagon
    std::cout <<"The perimeter is: {}cm" << format(perimeter) ;
    std::cin a = ((7 / 4) * pow(a, 2) * (1 / tan * (radians(180/7))));
    << ((7 / 4) * (a * *2) * (1 / tan * (radians(180)))) << std::end1;
}